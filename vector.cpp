#include<iostream>
#include<vector>
#include<string>
#include<utility>
#include<set>
#include<map>
using namespace std;

    int main()
    {
        //vector
          /* vector<int> v;
            for(int =0; i<5; i++)
            {
                v.push_back(i);

            }
              vector<int>;;iterator it;
                for(it=v.begin();  it!=v.end; it++)
                {
                    cout<<*it<<endl;

                } */
                   //string
                 /*  string s="rahul";
                    cout<<s<<endl;
                      string s1(s,2,4);
                      cout<<s1<<endl;
                         string s2=s.substr(2,4);
                          cout<<s2<<endl;
                            if(s1.compare(s2)==0)
                            {
                                cout<<s1<<" equal to "<<s2;
                            }
                               else{
                                                                cout<<s1<<" not equal to "<<s2;

                               }*/
                                 //pair
                              /*   pair<int,char> p;
                                 p=make_pair(3,'h');
                                 cout<<p.first<<" "<<p.second<<endl;
                                 pair<int,char> p2(4,'r');
                                cout<<p2.first<<" "<<p2.second<<endl;*/

                            //set
                              /* set<int> s;
                               int arr[] ={1,2,3,4,5,6,7,6,5,6};
                                 for(int i=0; i<10; i++)
                                 {
                                     s.insert(arr[i]);
                                 }
                                  set<int>::iterator it;
                                   for(it=s.begin(); it!=s.end(); it++)
                                   {
                                       cout<<*it<<endl;

                                   }
                                    if(s.find(9)==s.end())
                                    {
                                        cout<<"element not find "<<endl;

                                    }
                                      else
                                        cout<<"element find"<<endl;  */

                 //map

                          /*       int arr[]={1,2,3,4,5,4,3,2,4,3,7};
                                         map<int,int> m;
                                   for(int i=0; i<11; i++)
                                   {
                                       m[arr[i]]= m[arr[i]]+1;

                                   }

                                   map<int,int>::iterator it;

                                   for(it=m.begin(); it!=m.end(); it++)
                                   {
                                       cout<<it->first<<"-"<<it->second<<endl;

                                   }
                                    cout<<endl;

                                    m.erase(4);
                                      for(it=m.begin(); it!=m.end(); it++)
                                   {
                                       cout<<it->first<<"-"<<it->second<<endl;

                                   }
                                        */











    }
